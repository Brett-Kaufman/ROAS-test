
export interface Testimonial {
  headline: string;
  brand: string;
  category: string;
  description: string;
  fixHighlight: string;
  videoUrl?: string;
  embedCode?: string;
  posterUrl?: string;
}

export interface Capability {
  text: string;
}
